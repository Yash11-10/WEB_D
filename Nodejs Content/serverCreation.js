// const http = require('http');

// http.createServer((req, res)=>{
//     res.write("<h1>This is a server creation</h1>")
//     res.end("Hello"); //this is compulsory krna hi hai 
// }).listen(4800);

// -g = ye iska mtlb hai ki globally install kia hai nodemon (npm install -g nodemon esmei)
// -g flag installs an npm package globally so it can be used as a command anywhere in the system.
// can we crete multiple server in a same file = yes 









